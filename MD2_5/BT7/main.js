const scores = [1, 2, 3, 4, 5];

console.log("Bình phương:");
scores.forEach((s) => console.log(s * s));

const doubledScores = scores.map((s) => s * 2);
console.log("Gấp đôi:", doubledScores);
